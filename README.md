# 课表焕新

一个无需后端的课程表工作台：导入 CSV、JSON、复制文本或同类旧版 `.doc` 课表，生成周视图，并导出可导入 iPhone 日历的 `.ics` 文件。

## CSV 格式

CSV 第一行使用以下列名即可：

```text
课程名称,星期,开始节次,结束节次,教师,教室,周次
高等数学,星期一,1,2,张老师,J01A201,1-16
```

`周次` 支持 `1-4`、`2-4,9-12` 这样的连续区间。网页完全在浏览器本地解析文件，不上传课表内容。
